(library
 (name CamelotDB)
 (preprocess (pps ppx_deriving.show ppx_deriving.std)))

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --trace))
 
(env
 (dev
  (flags
   (:standard -warn-error -A))))

